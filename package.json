{
  "name": "ekavach-vapt-tracker",
  "version": "3.0.0",
  "private": true,
  "description": "eKavach VAPT Tracker - Enterprise Vulnerability Assessment & Penetration Testing Management Platform",
  "scripts": {
    "start": "bash ./start.sh",
    "setup": "bash ./setup.sh",
    "build": "cd frontend && npm run build",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
