# ============================================
# eKavach VAPT Tracker - Frontend Environment
# Copy to .env — DO NOT commit to version control.
# ============================================

# ── API (optional — auto-detects from window.location) ─────────────
# REACT_APP_API_PORT=5000          # Backend port (default: 5000)
# REACT_APP_API_URL=http://localhost:5000/api   # Force specific URL (overrides auto-detect)

# ── Socket.IO (optional) ─────────────────────────────────────────
# REACT_APP_SOCKET_URL=http://localhost:5000
